{
  "name": "not-fancy-birthday-checker",
  "version": "1.0.0",
  "description": "Countdown the days left for your birthday!",
  "scripts": {
    "build": "npx postcss *.css --use autoprefixer -d ."
  },
  "devDependencies": {
    "autoprefixer": "^9.1.3",
    "postcss-cli": "^6.0.0"
  }
}
